env         = "prod"
region      = "us-east-1"
root_domain = "whatsopdahl.com"
subdomain   = "megadraft"

# Google OAuth client ID (public value - used as the audience when verifying
# Google ID tokens). Get this from Google Cloud Console > Credentials.
google_client_id = "REPLACE_ME"
